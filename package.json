{
  "name": "mediadl",
  "version": "2.4.0",
  "description": "Multi-platform video and music downloader",
  "author": "kevclint",
  "type": "commonjs",
  "scripts": {
    "frontend:dev": "vite --host 127.0.0.1 --port 1420 --strictPort",
    "frontend:build": "vite build",
    "check:prereqs": "node scripts/check-build-prereqs.cjs",
    "start": "npm run check:prereqs && tauri dev",
    "dev": "npm run check:prereqs && tauri dev",
    "build": "npm run check:prereqs && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-react": "^5.1.4",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
